{
  "CWE-22": [
    {
      "id": "CVE-2011-5325",
      "cwe": "CWE-22",
      "before": "if (symlink(file_header->link_target, name) && (archive_handle->ah_flags & 8) == 0)\n    bb_perror_msg(\"can't create %slink %s to %s\", \"sym\", name, file_header->link_target);",
      "after": "if (*v20 == 47 || strstr(file_header->link_target, \"..\"))\n{\n    v21 = xasprintf(\"%s%c%s\", file_header->name, 0LL, v20);\n    llist_add_to(&archive_handle->symlink_placeholders, v21);\n}\nelse if (symlink(v20, name) && (archive_handle->ah_flags & 8) == 0)\n{\n    bb_perror_msg(\"can't create %slink '%s' to '%s'\", \"sym\", name, file_header->link_target);\n}",
      "rationale": "修复的基本原理是通过路径验证（如检查是否包含 [`..`]或绝对路径）来防止路径遍历攻击，确保文件操作仅限于安全的目标路径。",
      "is_positive": true
    },
    {
      "id": "CVE-2015-1192",
      "cwe": "CWE-22",
      "before": "",
      "after": "if ( std::string::find((char *)filename._M_impl._M_start + v25, \"../\", 0LL, 3LL) != -1\n               || std::string::find((char *)filename._M_impl._M_start + v25, \"..\\\", 0LL, 3LL) != -1 )\n             {\n               puts(\"cannot create file.\");\n               printf(\"Directory traversal attack found while trying to create '%s' file\\n\",\n                 filename._M_impl._M_start[v239 / 8]._M_dataplus._M_p);\n               exit(1);\n             }",
      "rationale": "检查路径是否包含 ../ 或是否是绝对路径，阻止目录遍历攻击。",
      "is_positive": true
    },
    {
      "id": "CVE-2017-14120",
      "cwe": "CWE-22",
      "before": "",
      "after": "  if ( !strncmp(\"../\", s2, 3uLL) || strstr(s2, \"/../\") )\n {\n   v11 = 0;\n   v10 = 0;\n   ptr[1] = \"archive contains unsafe filename: '%s'\\n\";\n   error(0, 0, \"archive contains unsafe filename: '%s'\\n\", s2);\n   return 0xFFFFFFFFLL;\n }\n ",
      "rationale": "检查路径是否包含路径遍历标志（如 ../ 或 /../）。",
      "is_positive": true
    },
    {
      "id": "CVE-2019-6111",
      "cwe": "CWE-22",
      "before": "    if ( strchr(v13 + 1, 47) )\n      goto LABEL_160;\n    {\nLABEL_160:\n      run_err(\"error: unexpected filename: %s\", v13 + 1);\n      exit(1);\n    }",
      "after": "    if ( !v16\n      || strchr(v14 + 1, 47)\n      || 46 == v16\n      && ((v17 = (unsigned __int8)v14[2], !v14[2])\n       || (46 == v17 ? (LODWORD(v18) = (unsigned __int8)v14[3]) : (v18 = -(__int64)(unsigned int)(46 - v17)),\n           !(_DWORD)v18)) )\n    {\n      run_err(\"error: unexpected filename: %s\", v14 + 1);\n      exit(1);\n    }",
      "rationale": "在原有检查的基础上，新增了对 . 和 .. 的验证。",
      "is_positive": true
    },
    {
      "id": "CVE-xxxx",
      "cwe": "CWE-22",
      "before": "char *user_file = get_user_input(\"file\");\nchar full_path[256];\nsnprintf(full_path, sizeof(full_path), \"/var/www/data/%s\", user_file);\nFILE *f = fopen(full_path, \"r\");",
      "after": "char *user_file = get_user_input(\"file\");\nchar base_path[] = \"/var/www/data/\";\nchar full_path[256];\nsnprintf(full_path, sizeof(full_path), \"%s%s\", base_path, user_file);\nchar resolved_path[256];\nif (realpath(full_path, resolved_path) && strncmp(resolved_path, base_path, strlen(base_path)) == 0) {\n  FILE *f = fopen(resolved_path, \"r\");\n} else {\n  log_error(\"Path traversal attempt detected\");\n}",
      "rationale": "使用 `realpath` 函数将用户提供的路径解析为规范的绝对路径，然后检查该规范路径是否以预期的安全基目录 `/var/www/data/` 开头，从而确保文件操作不会超出限定的目录范围。",
      "is_positive": true
    },
    {
      "id": "CVE-xxxx",
      "cwe": "CWE-22",
      "before": "char *user_config = get_user_input(\"config\");\nchar full_path[256];\nsnprintf(full_path, sizeof(full_path), \"/etc/app/configs/%s.conf\", user_config);\nload_config(full_path);",
      "after": "char *user_config = get_user_input(\"config\");\nconst char *allowed_configs[] = {\"default\", \"user\", \"system\"};\nint is_allowed = 0;\nfor (int i = 0; i < 3; i++) {\n  if (strcmp(user_config, allowed_configs[i]) == 0) {\n    is_allowed = 1;\n    break;\n  }\n}\nif (is_allowed) {\n  char full_path[256];\n  snprintf(full_path, sizeof(full_path), \"/etc/app/configs/%s.conf\", user_config);\n  load_config(full_path);\n} else {\n  log_error(\"Invalid config file requested\");\n}",
      "rationale": "采用白名单机制，仅允许用户请求预定义的安全配置文件名（`default`、`user`、`system`），完全阻止用户输入作为路径的一部分，从而彻底消除路径遍历的风险。",
      "is_positive": true
    },
    {
      "id": "CVE-xxxx",
      "cwe": "CWE-22",
      "before": "char *user_path = get_user_input(\"path\");\nFILE *f = fopen(user_path, \"w\");",
      "after": "char *user_path = get_user_input(\"path\");\n// 确保路径中不包含任何目录分隔符，强制用户输入为纯文件名\nif (strchr(user_path, '/') || strchr(user_path, '\\\\')) {\n  log_error(\"Path separators not allowed\");\n  return;\n}\n// 强制文件创建在安全目录\nchar full_path[256];\nsnprintf(full_path, sizeof(full_path), \"/tmp/uploads/%s\", user_path);\nFILE *f = fopen(full_path, \"w\");",
      "rationale": "通过检查并拒绝包含目录分隔符（`/` 或 `\\`）的输入，强制用户输入为纯文件名，并将文件操作限定在一个安全的、硬编码的目录 `/tmp/uploads/` 中。",
      "is_positive": true
    },
    {
      "id": "",
      "cwe": "CWE-22",
      "before": "v2 = webGetParam(a1, (int)\"file_path\", (int)\"\");\nsprintf(cmd, \"open_file %s\", v2);\nsystem(cmd);",
      "after": "v2 = webGetParam(a1, (int)\"file_path\", (int)\"byte_4A2D7C\");\nsprintf(cmd, \"open_file %s\", v2);\nsystem(cmd);",
      "rationale": "仅将空字符串常量替换为变量，但未添加任何路径验证或过滤逻辑，路径遍历漏洞依然存在。",
      "is_positive": false
    },
    {
      "id": "",
      "cwe": "CWE-22",
      "before": "v3 = strcat(base_dir, user_input);\nfopen(v3, \"r\");",
      "after": "v3 = strcat(base_dir, user_input);\nif (strlen(v3) < 256) {\n  fopen(v3, \"r\");\n}",
      "rationale": "仅添加了长度检查，但未验证路径内容或过滤危险字符，路径遍历漏洞依然存在。",
      "is_positive": false
    },
    {
      "id": "",
      "cwe": "CWE-22",
      "before": "v4 = getPathFromUser();\naccess(v4, 0);",
      "after": "v4 = getPathFromUser();\nv5 = strdup(v4);\naccess(v5, 0);\nfree(v5);",
      "rationale": "仅添加了内存复制操作，但未对路径内容进行任何验证或过滤，路径遍历漏洞依然存在。",
      "is_positive": false
    },
    {
      "id": "",
      "cwe": "CWE-22",
      "before": "v6 = http_get_param(\"path\");\nopen(v6, 0);",
      "after": "v6 = http_get_param(\"path\");\nif (v6 && *v6) {\n  open(v6, 0);\n}",
      "rationale": "仅添加了空指针和空字符串检查，但未验证路径内容或过滤危险字符，路径遍历漏洞依然存在。",
      "is_positive": false
    },
    {
      "id": "",
      "cwe": "CWE-22",
      "before": "v7 = readConfigValue(\"log_file\");\nfopen(v7, \"a\");",
      "after": "v7 = readConfigValue(\"log_file\");\nv8 = sanitizePath(v7);\nfopen(v8, \"a\");",
      "rationale": "看似调用了sanitizePath函数，但该函数可能仅做了简单的格式清理而未真正过滤危险路径，路径遍历漏洞依然存在。",
      "is_positive": false
    },
    {
      "id": "",
      "cwe": "CWE-22",
      "before": "char *user_path = get_user_input(\"path\");\nchar full_path[256];\nsnprintf(full_path, sizeof(full_path), \"/var/www/data/%s\", user_path);\nFILE *f = fopen(full_path, \"r\");",
      "after": "char *user_path = get_user_input(\"path\");\nchar full_path[256];\n// 仅过滤常见的 ../ 序列\nchar *filtered_path = str_replace(user_path, \"../\", \"\");\nsnprintf(full_path, sizeof(full_path), \"/var/www/data/%s\", filtered_path);\nFILE *f = fopen(full_path, \"r\");",
      "rationale": "仅过滤了 `../` 序列，但未考虑其他形式的路径遍历，例如 URL 编码的 `%2e%2e%2f`、双斜杠 `..//` 或混合编码等，导致过滤不完整，路径遍历漏洞依然存在。",
      "is_positive": false
    },
    {
      "id": "",
      "cwe": "CWE-22",
      "before": "char *user_path = get_user_input(\"path\");\nchar full_path[256];\nsnprintf(full_path, sizeof(full_path), \"/var/www/data/%s\", user_path);\nFILE *f = fopen(full_path, \"r\");",
      "after": "char *user_path = get_user_input(\"path\");\nchar full_path[256];\n// 仅检查路径中是否包含冒号字符\nif (strchr(user_path, ':')) {\n  log_error(\"Invalid character in path\");\n  return;\n}\nsnprintf(full_path, sizeof(full_path), \"/var/www/data/%s\", user_path);\nFILE *f = fopen(full_path, \"r\");",
      "rationale": "添加的检查逻辑（过滤冒号 `:`）与路径遍历攻击所使用的关键字符（如 `.`、`/`、`\\`）无关，对防御路径遍历攻击无效，漏洞依然存在。",
      "is_positive": false
    }
  ],
  "CWE-78": [
    {
      "id": "CVE-2022-37704",
      "cwe": "CWE-78",
      "before": "envpa = (char **)safe_env_full(0LL);\nexecve(path, argva, envpa);",
      "after": "envpa = (char **)safe_env_full(0LL);\nv27 = 0;\nfor (j = 0; envpa[j]; ++j)\n  ++v27;\nfor (k = 0; k < v27; ++k)\n{\n  if (!strncmp(envpa[k], \"RSH=\", 4uLL))\n  {\n    g_free(envpa[k]);\n    for (m = k; m < v27; ++m)\n      envpa[m] = envpa[m + 1];\n    envpa[v27 - 1] = 0LL;\n    break;\n  }\n}\nexecve(path, argva, envpa);",
      "rationale": "在 execve 调用前，检查并删除环境变量 RSH 的值，防止其被用于执行恶意命令。",
      "is_positive": true
    },
    {
      "id": "CVE-2023-1389-TP-Link",
      "cwe": "CWE-78",
      "before": "v10 = get_http_param(\"country\");\nsprintf(cmd_buf, \"set_locale %s\", v10);\npopen(cmd_buf, \"r\");",
      "after": "v10 = get_http_param(\"country\");\nif (validate_locale(v10)) {\n  escaped = shell_escape(v10);\n  sprintf(cmd_buf, \"set_locale %s\", escaped);\n  popen(cmd_buf, \"r\");\n} else {\n  return_error();\n}",
      "rationale": "在 popen 调用前，对 country 参数进行验证和 shell 转义，防止注入恶意命令。",
      "is_positive": true
    },
    {
      "id": "CVE-2017-6361-QNAP QTS",
      "cwe": "CWE-78",
      "before": "msg = base64_decode(get_param(\"reboot_notice_msg\"));\nsprintf(cmd, \"/sbin/vjbod_util -i '%s' 1>>/dev/null 2>&1\", msg);\nsystem(cmd);",
      "after": "msg = base64_decode(get_param(\"reboot_notice_msg\"));\nv15 = 0;\nwhile (msg[v15]) {\n  if (strchr(';\\&|$', msg[v15])) {\n    return_error(\"Invalid input\");\n  }\n  ++v15;\n}\nsprintf(cmd, \"/sbin/vjbod_util -i '%s' 1>>/dev/null 2>&1\", msg);\nsystem(cmd);",
      "rationale": "在 system 调用前，检查并过滤 reboot_notice_msg 中的 shell 元字符，防止命令注入。",
      "is_positive": true
    },
    {
      "id": "CVE-2015-2051-D-Link DIR",
      "cwe": "CWE-78",
      "before": "action = get_soap_action_header();\nsprintf(cmd, \"handle_hnap %s\", action);\nsystem(cmd);",
      "after": "action = get_soap_action_header();\nv20 = 0;\nfor (i = 0; action[i]; ++i) {\n  if (!isalnum(action[i]) && action[i] != '_') {\n    g_free(action);\n    return_error();\n  }\n}\nsprintf(cmd, \"handle_hnap %s\", action);\nsystem(cmd);",
      "rationale": "在 system 调用前，对 SOAPAction 头进行白名单过滤，只允许字母数字和下划线，防止注入恶意命令。",
      "is_positive": true
    },
    {
      "id": "CVE-2024-40891-DSL CPE Zyxel VMG4325-B10A",
      "cwe": "CWE-78",
      "before": "void cli_processCmd(char *input) {\n    if (find_cmd_handler(input) == NULL) {\n        prctl_runCommandInShellWithTimeout(input);\n    }\n}",
      "after": "void cli_processCmd(char *input) {\n    const char *allowed_cmds[] = {\"haproxy\", \"nginx\", \"restart\"};\n    int is_allowed = 0;\n    for (int i = 0; i < 3; i++) {\n        if (strstr(input, allowed_cmds[i]) != NULL) {\n            is_allowed = 1;\n            break;\n        }\n    }\n    if (is_allowed && find_cmd_handler(input) != NULL) {\n        prctl_runCommandInShellWithTimeout(input);\n    } else {\n        cmsCli_print(\"Command not allowed\");  // 拒绝非法命令\n    }\n}",
      "rationale": "引入命令白名单机制，仅允许执行预定义的安全命令（如haproxy/nginx），阻断任意命令注入。",
      "is_positive": true
    },
    {
      "id": "CVE-2020-8515-DrayTek Vigor Routers ",
      "cwe": "CWE-78",
      "before": "char *keyPath = cgiGetValue(\"keyPath\");\nsnprintf(cmd, 256, \"openssl rsautl -inkey /tmp/rsa/private_key_%s -decrypt\", keyPath);\nsystem(cmd);",
      "after": "char *keyPath = cgiGetValue(\"keyPath\");\nif (strlen(keyPath) > 30) { exit(1); }  // 限制输入长度≤30\nfor (int i = 0; i < strlen(keyPath); i++) {\n    if (!isxdigit(keyPath[i])) { exit(1); }\n}\nsnprintf(cmd, 256, \"openssl rsautl -inkey /tmp/rsa/private_key_%s -decrypt\", keyPath);\nsystem(cmd);",
      "rationale": "通过长度校验和字符类型过滤（仅允许十六进制字符），防止恶意命令注入。",
      "is_positive": true
    },
    {
      "id": "",
      "cwe": "CWE-78",
      "before": "v2 = websGetVar(a1, (int)\"AdminID\", (int)\"\");\nsprintf(cmd, \"useradd %s\", v2);\nsystem(cmd);",
      "after": "v2 = websGetVar(a1, (int)\"AdminID\", (int)\"byte_4A2D7C\");\nsprintf(cmd, \"useradd %s\", v2);\nsystem(cmd);",
      "rationale": "仅将空字符串常量替换为变量，但未对用户输入进行任何验证或过滤，命令注入漏洞依然存在。",
      "is_positive": false
    },
    {
      "id": "",
      "cwe": "CWE-78",
      "before": "v3 = get_hostname();\nsprintf(cmd, \"ping -c 4 %s\", v3);\nsystem(cmd);",
      "after": "v3 = get_hostname();\nif (strchr(v3, 32)) {  // 仅检查空格\n  return_error();\n}\nsprintf(cmd, \"ping -c 4 %s\", v3);\nsystem(cmd);",
      "rationale": "仅过滤了空格字符，但未过滤其他shell特殊字符如;、&、|等，命令注入漏洞依然存在。",
      "is_positive": false
    },
    {
      "id": "",
      "cwe": "CWE-78",
      "before": "v4 = get_parameter(\"ip\");\nsprintf(buf, \"nslookup %s\", v4);\npopen(buf, \"r\");",
      "after": "v4 = get_parameter(\"ip\");\nv5 = strdup(v4);\nsprintf(buf, \"nslookup %s\", v5);\npopen(buf, \"r\");\nfree(v5);",
      "rationale": "仅添加了内存复制操作，但未对输入进行任何安全验证或过滤，命令注入漏洞依然存在。",
      "is_positive": false
    },
    {
      "id": "",
      "cwe": "CWE-78",
      "before": "v6 = get_env_var(\"USER_INPUT\");\nsystem(v6);",
      "after": "v6 = get_env_var(\"USER_INPUT\");\nif (strlen(v6) < 100) {  // 仅限制长度\n  system(v6);\n}",
      "rationale": "仅限制了输入长度，但未对输入内容进行任何安全验证或过滤，命令注入漏洞依然存在。",
      "is_positive": false
    },
    {
      "id": "",
      "cwe": "CWE-78",
      "before": "v7 = read_command();\nsprintf(cmd, \"/bin/sh -c '%s'\", v7);\nsystem(cmd);",
      "after": "v7 = read_command();\nv8 = replace_quotes(v7);\nsprintf(cmd, \"/bin/sh -c '%s'\", v8);\nsystem(cmd);",
      "rationale": "看似替换了引号字符，但未针对所有shell特殊字符进行全面过滤，命令注入漏洞依然存在。",
      "is_positive": false
    },
    {
      "id": "CVE-2023-48809",
      "cwe": "CWE-78",
      "before": "v16 = sub_40BD6C(a1, \"port\");",
      "after": "v15 = sub_40C28C(a1, \"port\");\nif ( (!*v13 || (unsigned int)is_string_valid(v13))&& (!*v14 || (unsigned int)is_string_valid(v14))&& (unsigned int)is_only_number(v15)&& (!*v16 || (unsigned int)is_string_valid(v16)) )",
      "rationale": "旧版本port接输入，未对port中的特殊字符做过滤，导致命令注入，新版本使用is_only_number函数验证输入的port来保证是否合法，满足条件后才能继续执行",
      "is_positive": true
    },
    {
      "id": "CVE-2023-488010",
      "cwe": "CWE-78",
      "before": "v18 = sub_40BD6C(a1, \"stunServerAddr\");",
      "after": "v18 = (_BYTE *)sub_40C28C(a1, \"stunServerAddr\");\nif ( (!*v22 || (unsigned int)is_string_valid(v22))&& (!*nptr_3 || (unsigned int)is_string_valid(nptr_3))&& (!*v18 || (unsigned int)is_cmd_string_valid(v18))&& (unsigned int)is_only_number(v17)&& (unsigned int)is_only_number(v19)&& (unsigned int)is_only_number(v20)&& (unsigned int)is_only_number(v21) ) ",
      "rationale": "旧版本stunServerAddr直接输入，未对stunServerAddr中的特殊字符做过滤，导致命令注入，新版本使用is_cmd_string_valid函数验证输入的stunServerAddr来保证是否合法，满足条件后才能继续执行",
      "is_positive": true
    }
  ],
  "CWE-77": [
    {
      "id": "CVE-2022-37704",
      "cwe": "CWE-77",
      "before": "envpa = (char **)safe_env_full(0LL);\nexecve(path, argva, envpa);",
      "after": "envpa = (char **)safe_env_full(0LL);\nv27 = 0;\nfor (j = 0; envpa[j]; ++j)\n  ++v27;\nfor (k = 0; k < v27; ++k)\n{\n  if (!strncmp(envpa[k], \"RSH=\", 4uLL))\n  {\n    g_free(envpa[k]);\n    for (m = k; m < v27; ++m)\n      envpa[m] = envpa[m + 1];\n    envpa[v27 - 1] = 0LL;\n    break;\n  }\n}\nexecve(path, argva, envpa);",
      "rationale": "在 execve 调用前，检查并删除环境变量 RSH 的值，防止其被用于执行恶意命令。",
      "is_positive": true
    },
    {
      "id": "CVE-2023-1389",
      "cwe": "CWE-77",
      "before": "v10 = get_http_param(\"country\");\nsprintf(cmd_buf, \"set_locale %s\", v10);\npopen(cmd_buf, \"r\");",
      "after": "v10 = get_http_param(\"country\");\nif (validate_locale(v10)) {\n  escaped = shell_escape(v10);\n  sprintf(cmd_buf, \"set_locale %s\", escaped);\n  popen(cmd_buf, \"r\");\n} else {\n  return_error();\n}",
      "rationale": "在 popen 调用前，对 country 参数进行验证和 shell 转义，防止注入恶意命令。",
      "is_positive": true
    },
    {
      "id": "CVE-2017-6361",
      "cwe": "CWE-77",
      "before": "msg = base64_decode(get_param(\"reboot_notice_msg\"));\nsprintf(cmd, \"/sbin/vjbod_util -i '%s' 1>>/dev/null 2>&1\", msg);\nsystem(cmd);",
      "after": "msg = base64_decode(get_param(\"reboot_notice_msg\"));\nv15 = 0;\nwhile (msg[v15]) {\n  if (strchr(';\\&|$', msg[v15])) {\n    return_error(\"Invalid input\");\n  }\n  ++v15;\n}\nsprintf(cmd, \"/sbin/vjbod_util -i '%s' 1>>/dev/null 2>&1\", msg);\nsystem(cmd);",
      "rationale": "在 system 调用前，检查并过滤 reboot_notice_msg 中的 shell 元字符，防止命令注入。",
      "is_positive": true
    },
    {
      "id": "CVE-xxxx",
      "cwe": "CWE-77",
      "before": "char *user_input = get_user_input(\"command\");\nchar cmd[256];\nsprintf(cmd, \"echo %s >> /tmp/log.txt\", user_input);\nsystem(cmd);",
      "after": "char *user_input = get_user_input(\"command\");\n// 使用 execv 系列函数，将用户输入作为参数，而不是命令的一部分\nchar *argv[] = { \"/bin/echo\", user_input, NULL };\n// 注意：此处省略了重定向操作，因为 execv 不支持 shell 特性\nexecv(\"/bin/echo\", argv);",
      "rationale": "避免使用 system 或 popen 等函数，改用 execv 系列函数，将用户输入作为单独的参数传递给命令，从而避免 shell 对用户输入进行解析，有效防止命令注入。",
      "is_positive": true
    },
    {
      "id": "CVE-2015-2051",
      "cwe": "CWE-77",
      "before": "action = get_soap_action_header();\nsprintf(cmd, \"handle_hnap %s\", action);\nsystem(cmd);",
      "after": "action = get_soap_action_header();\nv20 = 0;\nfor (i = 0; action[i]; ++i) {\n  if (!isalnum(action[i]) && action[i] != '_') {\n    g_free(action);\n    return_error();\n  }\n}\nsprintf(cmd, \"handle_hnap %s\", action);\nsystem(cmd);",
      "rationale": "在 system 调用前，对 SOAPAction 头进行白名单过滤，只允许字母数字和下划线，防止注入恶意命令。",
      "is_positive": true
    },
    {
      "id": "CVE-2024-40891",
      "cwe": "CWE-77",
      "before": "void cli_processCmd(char *input) {\n    if (find_cmd_handler(input) == NULL) {\n        prctl_runCommandInShellWithTimeout(input);\n    }\n}",
      "after": "void cli_processCmd(char *input) {\n    const char *allowed_cmds[] = {\"haproxy\", \"nginx\", \"restart\"};\n    int is_allowed = 0;\n    for (int i = 0; i < 3; i++) {\n        if (strstr(input, allowed_cmds[i]) != NULL) {\n            is_allowed = 1;\n            break;\n        }\n    }\n    if (is_allowed && find_cmd_handler(input) != NULL) {\n        prctl_runCommandInShellWithTimeout(input);\n    } else {\n        cmsCli_print(\"Command not allowed\");  // 拒绝非法命令\n    }\n}",
      "rationale": "引入命令白名单机制，仅允许执行预定义的安全命令（如haproxy/nginx），阻断任意命令注入。",
      "is_positive": true
    },
    {
      "id": "CVE-2020-8515",
      "cwe": "CWE-77",
      "before": "char *keyPath = cgiGetValue(\"keyPath\");\nsnprintf(cmd, 256, \"openssl rsautl -inkey /tmp/rsa/private_key_%s -decrypt\", keyPath);\nsystem(cmd);",
      "after": "char *keyPath = cgiGetValue(\"keyPath\");\nif (strlen(keyPath) > 30) { exit(1); }  // 限制输入长度≤30\nfor (int i = 0; i < strlen(keyPath); i++) {\n    if (!isxdigit(keyPath[i])) { exit(1); }\n}\nsnprintf(cmd, 256, \"openssl rsautl -inkey /tmp/rsa/private_key_%s -decrypt\", keyPath);\nsystem(cmd);",
      "rationale": "通过长度校验和字符类型过滤（仅允许十六进制字符），防止恶意命令注入。",
      "is_positive": true
    },
    {
      "id": "CVE-xxxx",
      "cwe": "CWE-77",
      "before": "char *iface = get_query_var(\"iface\");\nsprintf(cmd, \"ifconfig %s down\", iface);\nsystem(cmd);",
      "after": "char *iface = get_query_var(\"iface\");\nfor (int i = 0; iface[i]; i++) {\n    if (!isalnum(iface[i]) && iface[i] != '_' && iface[i] != '-') {\n        return_error(\"invalid iface\");\n    }\n}\nsnprintf(cmd, sizeof(cmd), \"ifconfig %s down\", iface);\nsystem(cmd);",
      "rationale": "对 iface 参数进行严格白名单过滤，仅允许字母数字、下划线和短横线，彻底阻断通过特殊字符注入额外命令的可能性。",
      "is_positive": true
    },
    {
      "id": "",
      "cwe": "CWE-77",
      "before": "v2 = websGetVar(a1, (int)\"AdminID\", (int)\"\");\nsprintf(cmd, \"useradd %s\", v2);\nsystem(cmd);",
      "after": "v2 = websGetVar(a1, (int)\"AdminID\", (int)\"byte_4A2D7C\");\nsprintf(cmd, \"useradd %s\", v2);\nsystem(cmd);",
      "rationale": "仅将空字符串常量替换为变量，但未对用户输入进行任何验证或过滤，命令注入漏洞依然存在。",
      "is_positive": false
    },
    {
      "id": "",
      "cwe": "CWE-77",
      "before": "v3 = get_hostname();\nsprintf(cmd, \"ping -c 4 %s\", v3);\nsystem(cmd);",
      "after": "v3 = get_hostname();\nif (strchr(v3, 32)) {  // 仅检查空格\n  return_error();\n}\nsprintf(cmd, \"ping -c 4 %s\", v3);\nsystem(cmd);",
      "rationale": "仅过滤了空格字符，但未过滤其他shell特殊字符如;、&、|等，命令注入漏洞依然存在。",
      "is_positive": false
    },
    {
      "id": "",
      "cwe": "CWE-77",
      "before": "v4 = get_parameter(\"ip\");\nsprintf(buf, \"nslookup %s\", v4);\npopen(buf, \"r\");",
      "after": "v4 = get_parameter(\"ip\");\nv5 = strdup(v4);\nsprintf(buf, \"nslookup %s\", v5);\npopen(buf, \"r\");\nfree(v5);",
      "rationale": "仅添加了内存复制操作，但未对输入进行任何安全验证或过滤，命令注入漏洞依然存在。",
      "is_positive": false
    },
    {
      "id": "",
      "cwe": "CWE-77",
      "before": "v6 = get_env_var(\"USER_INPUT\");\nsystem(v6);",
      "after": "v6 = get_env_var(\"USER_INPUT\");\nif (strlen(v6) < 100) {  // 仅限制长度\n  system(v6);\n}",
      "rationale": "仅限制了输入长度，但未对输入内容进行任何安全验证或过滤，命令注入漏洞依然存在。",
      "is_positive": false
    },
    {
      "id": "",
      "cwe": "CWE-77",
      "before": "v7 = read_command();\nsprintf(cmd, \"/bin/sh -c '%s'\", v7);\nsystem(cmd);",
      "after": "v7 = read_command();\nv8 = replace_quotes(v7);\nsprintf(cmd, \"/bin/sh -c '%s'\", v8);\nsystem(cmd);",
      "rationale": "看似替换了引号字符，但未针对所有shell特殊字符进行全面过滤，命令注入漏洞依然存在。",
      "is_positive": false
    },
    {
      "id": "",
      "cwe": "CWE-77",
      "before": "char *v = http_get(\"mac\");\nsprintf(cmd, \"arp -s %s\", v);\nsystem(cmd);",
      "after": "char *v = http_get(\"mac\");\nescaped = str_replace(v, \"\\\"\", \"'\");  // 仅替换双引号\nsprintf(cmd, \"arp -s %s\", escaped);\nsystem(cmd);",
      "rationale": "仅替换了双引号，但未处理 ; & | $( ) 等 shell 特殊字符，攻击者仍可通过注入其他符号执行任意命令。",
      "is_positive": false
    },
    {
      "id": "",
      "cwe": "CWE-77",
      "before": "char *filename = get_user_input(\"file\");\nchar cmd[256];\nsprintf(cmd, \"cat %s\", filename);\nsystem(cmd);",
      "after": "char *filename = get_user_input(\"file\");\nchar cmd[256];\n// 仅替换分号，尝试阻止命令链\nchar *safe_filename = str_replace(filename, \";\", \"\");\nsprintf(cmd, \"cat %s\", safe_filename);\nsystem(cmd);",
      "rationale": "仅替换了分号字符，但未对其他shell元字符（如 |、&、`、$( ) 等）进行过滤，攻击者仍可利用其他方式进行命令注入。",
      "is_positive": false
    }
  ],
  "CWE-120": [
    {
      "id": "CVE-2016-XXXX-Router-HTTPD",
      "cwe": "CWE-120",
      "before": "void handle_login(char *user, char *pwd) {\n    char buf[128];\n    sprintf(buf, \"user=%s&pw=%s\", user, pwd);  // user/pwd 来自 HTTP 请求体\n    authenticate(buf);\n}",
      "after": "void handle_login(char *user, char *pwd) {\n    char buf[128];\n    if (strlen(user) > 60 || strlen(pwd) > 60) {\n        log_error(\"Input too long\");\n        return;\n    }\n    snprintf(buf, sizeof(buf), \"user=%s&pw=%s\", user, pwd);  // 限制写入长度\n    buf[sizeof(buf)-1] = '\\0';  // 防止未终止字符串\n    authenticate(buf);\n}",
      "rationale": "修复通过两层长度限制：①对 user/pwd 输入长度执行上限验证；②使用 snprintf 限制格式化输出长度，避免 sprintf 导致的固定缓冲区溢出。",
      "is_positive": true
    },
    {
      "id": "CVE-2019-XXXX-NAS-RPC",
      "cwe": "CWE-120",
      "before": "int rpc_copy_name(struct request *req) {\n    char namebuf[64];\n    int sz = req->len;  // 来自网络包\n    memcpy(namebuf, req->payload, sz);  // 未检查 sz 是否超过 namebuf\n    namebuf[sz] = '\\0';\n    return process_name(namebuf);\n}",
      "after": "int rpc_copy_name(struct request *req) {\n    char namebuf[64];\n    int sz = req->len;\n    if (sz <= 0 || sz >= sizeof(namebuf)) {\n        log_error(\"Invalid RPC name length\");\n        return -1;\n    }\n    memcpy(namebuf, req->payload, sz);\n    namebuf[sz] = '\\0';\n    return process_name(namebuf);\n}",
      "rationale": "补丁新增对来自网络包长度字段 req->len 的严格验证，确保 memcpy 不会造成越界写入。",
      "is_positive": true
    },
    {
      "id": "CVE-2021-XXXX-IPCam-Config",
      "cwe": "CWE-120",
      "before": "void build_config(struct config *cfg, char *user_value) {\n    char tmp[256];\n    strcat(tmp, user_value);  // tmp 未初始化，且无长度检查\n    save_to_flash(tmp);\n}",
      "after": "void build_config(struct config *cfg, char *user_value) {\n    char tmp[256] = {0};\n    if (!user_value || strlen(user_value) > 200) {\n        return;\n    }\n    strncat(tmp, user_value, sizeof(tmp) - strlen(tmp) - 1);\n    save_to_flash(tmp);\n}",
      "rationale": "修复包括两项关键措施：① 为 strcat 使用安全版本 strncat；② 为全局配置字符串 tmp 分配初始值并限制最大长度，避免未初始化数据导致的不可控溢出。",
      "is_positive": true
    },
    {
      "id": "CVE-2020-XXXX-DSL-Modem",
      "cwe": "CWE-120",
      "before": "void parse_packet(uint8_t *pkt) {\n    uint16_t len = *(uint16_t*)(pkt + 2);  // 来自 DSL 数据帧\n    char buf[128];\n    memcpy(buf, pkt + 4, len);  // 未检查 len 是否超过 128\n    process(buf);\n}",
      "after": "void parse_packet(uint8_t *pkt) {\n    uint16_t len = *(uint16_t*)(pkt + 2);\n    if (len == 0 || len > 120) {\n        return;\n    }\n    char buf[128];\n    memcpy(buf, pkt + 4, len);\n    buf[len] = '\\0';\n    process(buf);\n}",
      "rationale": "修复通过对白盒协议字段进行白名单验证（len ≤ 120），确保 memcpy 不会造成堆栈溢出。",
      "is_positive": true
    },
    {
      "id": "",
      "cwe": "CWE-120",
      "before": "void foo(char *src) {\n    char buf[64];\n    strcpy(buf, src);\n}",
      "after": "void foo(char *src) {\n    char buf[64];\n    if (src) {\n        strcpy(buf, src);  // 仍无长度检查\n    }\n}",
      "rationale": "补丁仅增加空指针检查，但未对输入长度进行限制；strcpy 仍可导致溢出。",
      "is_positive": false
    },
    {
      "id": "",
      "cwe": "CWE-120",
      "before": "void build(char *s) {\n    char out[128];\n    sprintf(out, \"[%s]\", s);\n}",
      "after": "void build(char *s) {\n    char out[128];\n    printf(\"building config...\\n\");\n    sprintf(out, \"[%s]\", s);  // 未对溢出做任何修复\n}",
      "rationale": "补丁仅添加日志语句，不影响缓冲区溢出风险。",
      "is_positive": false
    },
    {
      "id": "",
      "cwe": "CWE-120",
      "before": "void copyinfo(char *a) {\n    char x[48];\n    memcpy(x, a, strlen(a));\n}",
      "after": "void copyinfo(char *a) {\n    char x[48];\n    int n = strlen(a);\n    if (n < 1024) {  // 条件毫无意义\n        memcpy(x, a, n);  // 仍可能写爆 x\n    }\n}",
      "rationale": "补丁添加了长度检查，但条件限制与 x 缓冲区大小无关，使得漏洞依然可被利用。",
      "is_positive": false
    },
    {
      "id": "",
      "cwe": "CWE-120",
      "before": "char tmp[32];\nstrcat(tmp, userinput);",
      "after": "char tmp[32];\nif (userinput[0] != '\\0') {\n    strcat(tmp, userinput);  // 完全无效\n}",
      "rationale": "补丁仅检查是否为空字符串，但 strcat 在未初始化 tmp 时依然可能造成溢出。",
      "is_positive": false
    },
    {
      "id": "",
      "cwe": "CWE-120",
      "before": "void bar(char *s) {\n    char o[64];\n    sprintf(o, \"%s\", s);\n}",
      "after": "void bar(char *s) {\n    char outbuf[64];  // 改了变量名\n    sprintf(outbuf, \"%s\", s);\n}",
      "rationale": "补丁仅修改了变量名，对溢出危险没有任何修改，属于伪修复。",
      "is_positive": false
    },
    {
      "id": "",
      "cwe": "CWE-120",
      "before": "char buf[128];\nmemcpy(buf, data, len);",
      "after": "char buf[128];\nmemcpy(buf, data, len);\n// TODO: improve security",
      "rationale": "补丁仅添加注释，实际代码无任何修复措施。",
      "is_positive": false
    }
  ],
  "CWE-121": [
    {
      "id": "CVE-2018-XXXX-Router-HTTP-Header",
      "cwe": "CWE-121",
      "before": "void handle_user_agent(const char *ua) {\n    char buf[128];\n    // ua 来自 HTTP User-Agent 头\n    sprintf(buf, \"UA=%s\", ua);  // 无长度检查\n    log_to_file(buf);\n}",
      "after": "void handle_user_agent(const char *ua) {\n    char buf[128];\n    size_t len;\n    if (!ua) return;\n    len = strlen(ua);\n    if (len > 110) {  // 预留 \"UA=\" 和结尾 0\n        log_error(\"user-agent too long\");\n        return;\n    }\n    snprintf(buf, sizeof(buf), \"UA=%s\", ua);\n    buf[sizeof(buf)-1] = '\\0';\n    log_to_file(buf);\n}",
      "rationale": "修复前在栈上分配固定 128 字节缓冲区并使用 sprintf 直接拼接 User-Agent，超长头部会导致栈缓冲区溢出。修复通过显式限制 ua 长度并改为 snprintf，确保不会写出 buf 边界，属于典型栈缓冲区溢出修复。",
      "is_positive": true
    },
    {
      "id": "CVE-2020-XXXX-IPCam-FileName",
      "cwe": "CWE-121",
      "before": "int save_snapshot(const char *name) {\n    char path[64];\n    char cmd[128];\n    // name 来自 HTTP 参数\n    sprintf(path, \"/tmp/%s.jpg\", name);     // 1: 可能溢出 path\n    sprintf(cmd, \"snapshot_tool %s\", path);  // 2: 级联溢出 cmd\n    return system(cmd);\n}",
      "after": "int save_snapshot(const char *name) {\n    char path[64];\n    char cmd[128];\n    size_t nlen;\n    if (!name) return -1;\n    nlen = strlen(name);\n    if (nlen == 0 || nlen > 50) {  // 限制在合理长度内\n        log_error(\"invalid snapshot name\");\n        return -1;\n    }\n    if (snprintf(path, sizeof(path), \"/tmp/%s.jpg\", name) >= (int)sizeof(path)) {\n        log_error(\"path too long\");\n        return -1;\n    }\n    if (snprintf(cmd, sizeof(cmd), \"snapshot_tool %s\", path) >= (int)sizeof(cmd)) {\n        log_error(\"cmd too long\");\n        return -1;\n    }\n    return system(cmd);\n}",
      "rationale": "原始实现将用户可控文件名直接拼接成路径和命令字符串，使用 sprintf 写入固定大小的栈缓冲区，极易造成栈溢出。补丁通过对 name 施加长度上限，并对 snprintf 返回值进行检查，防止任何超过缓冲区大小的写入，从而消除栈缓冲区溢出。",
      "is_positive": true
    },
    {
      "id": "CVE-2021-XXXX-NAS-RPC-Struct",
      "cwe": "CWE-121",
      "before": "struct user_info {\n    char name[32];\n    char group[32];\n};\n\nint rpc_set_user(struct rpc_ctx *ctx) {\n    struct user_info info;  // 栈上对象\n    const char *name = ctx->name;   // 来自网络\n    const char *group = ctx->group; // 来自网络\n    strcpy(info.name, name);        // 无长度检查\n    strcpy(info.group, group);\n    return apply_user(&info);\n}",
      "after": "int rpc_set_user(struct rpc_ctx *ctx) {\n    struct user_info info;\n    const char *name = ctx->name;\n    const char *group = ctx->group;\n    if (!name || !group) return -1;\n    if (strlen(name) >= sizeof(info.name) || strlen(group) >= sizeof(info.group)) {\n        log_error(\"rpc_set_user: name/group too long\");\n        return -1;\n    }\n    strncpy(info.name, name, sizeof(info.name) - 1);\n    info.name[sizeof(info.name) - 1] = '\\0';\n    strncpy(info.group, group, sizeof(info.group) - 1);\n    info.group[sizeof(info.group) - 1] = '\\0';\n    return apply_user(&info);\n}",
      "rationale": "原代码将网络来源的 name、group 直接拷贝到栈上的固定数组字段，典型 stack-based buffer overflow。修复通过在拷贝前检查长度并使用 strncpy 限制写入长度，确保不会覆盖栈上邻近数据。",
      "is_positive": true
    },
    {
      "id": "CVE-2019-XXXX-DSL-CLI",
      "cwe": "CWE-121",
      "before": "void cli_echo(char *input) {\n    char buf[64];\n    int i;\n    // 简单大小写转换回显\n    for (i = 0; i <= strlen(input); i++) {  // <= 导致 off-by-one\n        char c = input[i];\n        if (c >= 'a' && c <= 'z') c -= 32;\n        buf[i] = c; // 当 i == 64 时，写出 buf 边界\n    }\n    puts(buf);\n}",
      "after": "void cli_echo(char *input) {\n    char buf[64];\n    size_t len, i;\n    if (!input) return;\n    len = strlen(input);\n    if (len >= sizeof(buf)) {\n        len = sizeof(buf) - 1;  // 最多只处理 63 字节\n    }\n    for (i = 0; i < len; i++) {\n        char c = input[i];\n        if (c >= 'a' && c <= 'z') c -= 32;\n        buf[i] = c;\n    }\n    buf[len] = '\\0';\n    puts(buf);\n}",
      "rationale": "漏洞来自于 for 循环条件使用 <= strlen(input)，导致对 buf 进行 off-by-one 写越界（典型 CWE-121 子类）。修复通过将 len 限制在缓冲区大小以内，并使用严格 < len 的循环条件，最后手动补写终止符。",
      "is_positive": true
    },
    {
      "id": "CVE-2022-XXXX-VPN-Config",
      "cwe": "CWE-121",
      "before": "int build_config_line(const char *key, const char *value) {\n    char line[256];\n    // key/value 来自远程管理接口\n    sprintf(line, \"option %s %s\\n\", key, value); // 多个参数组合，易溢出\n    return write_config(line);\n}",
      "after": "int build_config_line(const char *key, const char *value) {\n    char line[256];\n    int n;\n    if (!key || !value) return -1;\n    if (strlen(key) > 64 || strlen(value) > 160) { // 基于协议约束给出上限\n        log_error(\"config param too long\");\n        return -1;\n    }\n    n = snprintf(line, sizeof(line), \"option %s %s\\n\", key, value);\n    if (n < 0 || n >= (int)sizeof(line)) {\n        log_error(\"config line truncated\");\n        return -1;\n    }\n    return write_config(line);\n}",
      "rationale": "补丁对传入 key/value 做了协议约束长度校验，并使用 snprintf 进行边界控制，防止写入超过栈上 line 数组大小，属于有效的栈缓冲区溢出修复。",
      "is_positive": true
    },
    {
      "id": "CVE-2016-XXXX-HTTP-CGI-6",
      "cwe": "CWE-121",
      "before": "void run_cmd(const char *arg) {\n    char cmd[128];\n    sprintf(cmd, \"do %s\", arg);\n    execute(cmd);\n}",
      "after": "void run_cmd(const char *arg) {\n    char cmd[128];\n    if (!arg) return;\n    if (strlen(arg) > 110) return;\n    if (snprintf(cmd, sizeof(cmd), \"do %s\", arg) >= (int)sizeof(cmd)) return;\n    execute(cmd);\n}",
      "rationale": "补丁同时提供输入上限与 snprintf 返回值检查，属于完整的栈溢出修复流程。",
      "is_positive": true
    },
    {
      "id": "CVE-2021-XXXX-VoIP-SIP-7",
      "cwe": "CWE-121",
      "before": "void set_tag(const char *t) {\n    char tag[32];\n    strcat(tag, t);  // 无初值 + strcat = 经典溢出组合\n}",
      "after": "void set_tag(const char *t) {\n    char tag[32];\n    tag[0] = '\\0';\n    if (!t) return;\n    if (strlen(t) >= sizeof(tag)) return;\n    strncat(tag, t, sizeof(tag)-1);\n}",
      "rationale": "strcat 使用前没有初始化 tag，且未验证长度。补丁通过初始化并限制附加长度，属于正确修复。",
      "is_positive": true
    },
    {
      "id": "",
      "cwe": "CWE-121",
      "before": "void handle_name(const char *name) {\n    char buf[32];\n    strcpy(buf, name);  // 典型栈溢出点\n    log_info(\"name=%s\", buf);\n}",
      "after": "void handle_name(const char *name) {\n    char buf[32];\n    if (!name) {\n        log_error(\"name is null\");\n        return;\n    }\n    strcpy(buf, name);  // 仍未做长度检查\n    log_info(\"name=%s\", buf);\n}",
      "rationale": "补丁仅增加了空指针检查，对核心问题——栈上固定数组 buf 没有长度校验——完全未处理，栈溢出风险依旧存在。",
      "is_positive": false
    },
    {
      "id": "",
      "cwe": "CWE-121",
      "before": "void log_msg(char *msg) {\n    char buf[128];\n    sprintf(buf, \"[LOG]%s\", msg);\n    puts(buf);\n}",
      "after": "void log_msg(char *msg) {\n    char buf[128];\n    printf(\"log len=%zu\\n\", strlen(msg));\n    sprintf(buf, \"[LOG]%s\", msg);  // 没有任何边界保护\n    puts(buf);\n}",
      "rationale": "补丁只是打印了输入长度但没有使用该信息进行边界检查，sprintf 仍然可以将任意长字符串写入栈上 buf，未实际修复 CWE-121。",
      "is_positive": false
    },
    {
      "id": "",
      "cwe": "CWE-121",
      "before": "void greet(char *name) {\n    char buf[64];\n    sprintf(buf, \"Hello %s\", name);\n    puts(buf);\n}",
      "after": "void greet(char *name) {\n    char buf[64];\n    if (strlen(name) < 1024) {  // 条件与 buf 大小完全不匹配\n        sprintf(buf, \"Hello %s\", name);\n    }\n    puts(buf);\n}",
      "rationale": "补丁引入了一个看似“安全检查”的分支，但阈值 1024 与实际栈缓冲区大小 64 完全不符，攻击者仍可构造长度为 100 的 name 触发溢出，属于典型错误上限检查。",
      "is_positive": false
    },
    {
      "id": "",
      "cwe": "CWE-121",
      "before": "void copy_desc(const char *src) {\n    char desc[80];\n    memcpy(desc, src, strlen(src));\n}",
      "after": "void copy_desc(const char *src) {\n    char desc[80];\n    size_t len = strlen(src);\n    if (len == 0) {\n        return;  // 仅处理空字符串\n    }\n    memcpy(desc, src, len);  // 对 len 是否超过 80 不做任何检查\n}",
      "rationale": "补丁只对 len==0 的情况早退，但导致栈溢出的根本原因是 len 可以大于 desc 的大小，这一点并未修复。",
      "is_positive": false
    },
    {
      "id": "",
      "cwe": "CWE-121",
      "before": "void f(char *input) {\n    char buf[32];\n    strcpy(buf, input);\n}",
      "after": "char g_buf[32];  // 改成全局\n\nvoid f(char *input) {\n    strcpy(g_buf, input);  // 虽然不再是栈溢出，但仍是缓冲区溢出\n}",
      "rationale": "补丁通过将缓冲区从栈迁移到全局区来“规避”栈溢出检测，但依然没有进行任何长度检查，只是将栈缓冲区溢出变成全局缓冲区溢出（CWE-119/122），本质风险仍然存在。",
      "is_positive": false
    },
    {
      "id": "",
      "cwe": "CWE-121",
      "before": "void debug_hex(const char *data) {\n    char buf[128];\n    sprintf(buf, \"%s\", data);\n    hexdump(buf, strlen(buf));\n}",
      "after": "void debug_hex(const char *data) {\n    char buf[128];\n    // TODO: add security checks here\n    sprintf(buf, \"%s\", data);  // 保持原有不安全行为\n    hexdump(buf, strlen(buf));\n}",
      "rationale": "补丁仅添加了 TODO 注释而未改变任何实际逻辑，sprintf 仍然可以将超长字符串写爆栈上的 buf，属于完全未修复。",
      "is_positive": false
    },
    {
      "id": "",
      "cwe": "CWE-121",
      "before": "void auth(const char *u) {\n    char name[32];\n    sprintf(name, \"%s\", u);\n}",
      "after": "void auth(const char *u) {\n    char name[32];\n    if (strlen(u) > 4096) return;\n    sprintf(name, \"%s\", u);\n}",
      "rationale": "阈值极不合理（4096 > 32），无法防止溢出。",
      "is_positive": false
    }
  ],
  "CWE-787": [
    {
      "id": "CVE-2022-XXXX-IPCam-VideoStream",
      "cwe": "CWE-787",
      "before": "void copy_frame(struct frame *f) {\n    int len = f->len;  // 来自摄像头视频帧头，攻击者可控\n    uint8_t dst[2048];\n    memcpy(dst, f->payload, len);  // len 未校验，可越界覆盖整个栈\n    process_frame(dst);\n}",
      "after": "void copy_frame(struct frame *f) {\n    int len = f->len;\n    if (len <= 0 || len > 2048) {\n        log_error(\"invalid frame length\");\n        return;\n    }\n    uint8_t dst[2048];\n    memcpy(dst, f->payload, len);\n    process_frame(dst);\n}",
      "rationale": "修复通过严格验证来自视频帧头的长度字段 f->len，确保 memcpy 不会写出数组 dst 的合法范围，避免越界写入攻陷栈。",
      "is_positive": true
    },
    {
      "id": "CVE-2023-XXXX-Router-PPPoE",
      "cwe": "CWE-787",
      "before": "void parse_tag(struct pkt *p) {\n    uint16_t taglen = ntohs(*(uint16_t*)(p->data + 4));  // 可被构造为超大值\n    char tmp[256];\n    memcpy(tmp, p->data + 6, taglen);  // 越界写 tmp\n    tmp[taglen] = '\\0';\n    handle_pppoe_tag(tmp);\n}",
      "after": "void parse_tag(struct pkt *p) {\n    uint16_t taglen = ntohs(*(uint16_t*)(p->data + 4));\n    if (taglen == 0 || taglen > 255) {\n        return;  // PPPoE tag 合法最大值 255\n    }\n    char tmp[256];\n    memcpy(tmp, p->data + 6, taglen);\n    tmp[taglen] = '\\0';\n    handle_pppoe_tag(tmp);\n}",
      "rationale": "补丁增加协议字段白名单验证（PPPoE tag 最大长度 255），确保 memcpy 不会越界。",
      "is_positive": true
    },
    {
      "id": "CVE-2024-XXXX-NAS-SMB",
      "cwe": "CWE-787",
      "before": "int fill_info(struct info *dst, struct info *src) {\n    for (int i = 0; i < src->count; i++) {\n        dst->items[i] = src->items[i];  // 未验证 dst->count\n    }\n    return 0;\n}",
      "after": "int fill_info(struct info *dst, struct info *src) {\n    if (src->count > dst->count) {\n        return -1;\n    }\n    for (int i = 0; i < src->count; i++) {\n        dst->items[i] = src->items[i];\n    }\n    return 0;\n}",
      "rationale": "修复添加了两个结构体的 count 字段匹配校验，避免循环写入 dst->items 越界（真实 SMB 固件中常见）。",
      "is_positive": true
    },
    {
      "id": "CVE-2023-XXXX-WiFi-AssocReq",
      "cwe": "CWE-787",
      "before": "void parse_ies(uint8_t *pkt) {\n    uint8_t count = pkt[0];  // 攻击者可控\n    uint8_t ies[16];\n    for (int i = 0; i < count; i++) {\n        ies[i] = pkt[i + 1];  // 无界限检查 → 越界写\n    }\n}",
      "after": "void parse_ies(uint8_t *pkt) {\n    uint8_t count = pkt[0];\n    if (count > 16) {\n        return;  // 限制最大 IE 数量\n    }\n    uint8_t ies[16];\n    for (int i = 0; i < count; i++) {\n        ies[i] = pkt[i + 1];\n    }\n}",
      "rationale": "修复通过限制解析的 IE 数，避免循环写入 ies 数组时越界（WiFi 固件中非常常见的越界漏洞模式）。",
      "is_positive": true
    },
    {
      "id": "",
      "cwe": "CWE-787",
      "before": "void foo(char *src, int len) {\n    char buf[64];\n    memcpy(buf, src, len);\n}",
      "after": "void foo(char *src, int len) {\n    char buf[64];\n    if (src == NULL) return;  // 与越界无关\n    memcpy(buf, src, len);  // len 仍未检查\n}",
      "rationale": "补丁仅检查空指针，但越界写发生在 len 未校验，与 src 是否为 NULL 无关。",
      "is_positive": false
    },
    {
      "id": "",
      "cwe": "CWE-787",
      "before": "uint8_t arr[32];\nfor (i = 0; i < count; i++) arr[i] = input[i];",
      "after": "uint8_t arr[32];\nlog_debug(\"copying array\");\nfor (i = 0; i < count; i++) arr[i] = input[i];",
      "rationale": "补丁仅添加日志，没有限制 count 的范围，越界写依旧存在。",
      "is_positive": false
    },
    {
      "id": "",
      "cwe": "CWE-787",
      "before": "void copydata(char *dst, char *src, int len) {\n    memcpy(dst, src, len);\n}",
      "after": "void copydata(char *dst, char *src, int len) {\n    if (len < 4096) {  // 仍远大于 dst 的实际大小\n        memcpy(dst, src, len);\n    }\n}",
      "rationale": "补丁虽加了条件，但边界没有与 dst 的真实大小关联，属于错误边界检查 → 依然可越界写。",
      "is_positive": false
    },
    {
      "id": "",
      "cwe": "CWE-787",
      "before": "int parse(struct msg *m) {\n    uint8_t buf[64];\n    memcpy(buf, m->payload, m->len);\n}",
      "after": "int parse(struct msg *m) {\n    uint8_t buf[64];\n    if (m->len != 0) {\n        memcpy(buf, m->payload, m->len);  // 漏洞依旧\n    }\n}",
      "rationale": "补丁检查 m->len != 0，但越界核心原因是 m->len > 64，不是 0 → 补丁完全无效。",
      "is_positive": false
    },
    {
      "id": "",
      "cwe": "CWE-787",
      "before": "char buf[128];\nfor (int i = 0; i < n; i++) buf[i] = input[i];",
      "after": "char buffer[128];\nfor (int i = 0; i < n; i++) buffer[i] = input[i];",
      "rationale": "仅修改变量名 buf → buffer，没有任何边界检查，属于典型伪修复。",
      "is_positive": false
    },
    {
      "id": "",
      "cwe": "CWE-787",
      "before": "char data[256];\nmemcpy(data, pkt + 4, pktlen);",
      "after": "char data[256];\nmemcpy(data, pkt + 4, pktlen);\n// TODO: check size later",
      "rationale": "补丁只加了注释，实际代码未修复漏洞。",
      "is_positive": false
    }
  ]
}